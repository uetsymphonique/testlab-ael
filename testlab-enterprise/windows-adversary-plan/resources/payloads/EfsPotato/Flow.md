# CertEnrollSvc — Flow

**Entry:** `CertEnrollmentAgent.Main()` · **Artifact summary:** SYSTEM child process spawned via EFS named-pipe coercion; optional temp PE file written then deleted

| # | Behavior (`actor action artifact`) | Artifact [class] → consumed by | Tactic / TID — Technique Name | Context (baseline) |
|---|---|---|---|---|
| 1 | CertEnrollSvc.exe resolves all sensitive Win32 APIs at runtime via GetProcAddress; advapi32 and Rpcrt4 loaded via LoadLibraryW | — [no-artifact] → #4 #5 #6 #7 #8 | Defense Evasion / T1027.007 — Obfuscated Files or Information: Dynamic API Resolution | only GetModuleHandleW / GetProcAddress appear in static IAT; import table analysis yields no sensitive API names |
| 2 | CertEnrollSvc.exe allocates a private page via VirtualAlloc and flips it to PAGE_EXECUTE_READ via VirtualProtect, then writes indirect syscall trampolines (NtFsControlFile, NtOpenThreadToken, NtDuplicateToken) into it | executable private page [memory] → #7 | Defense Evasion / T1620 — Reflective Code Loading | VAD entry with EXECUTE_READ and no file backing; normal code sits in MEM_IMAGE regions |
| 3 | CertEnrollSvc.exe reads a framed PE (4-byte LE size header + PE bytes) from stdin and writes it to a %TEMP% file | temp PE file [file] → #8 | Execution / T1106 — Native API | PE written to temp path by a non-installer process (stdin delivery, conditional path) |
| 4 | CertEnrollSvc.exe enables SeImpersonatePrivilege on its own access token via AdjustTokenPrivileges | token state [identity] → #7 | Privilege Escalation / T1134.001 — Access Token Manipulation: Token Impersonation/Theft | AdjustTokenPrivileges on self; IIS AppPool identities hold SeImpersonatePrivilege by default, so this is mostly a no-op enablement |
| 5 | CertEnrollSvc.exe creates named pipe `\\.\pipe\{GUID}<suffix>` via CreateNamedPipeW | named pipe endpoint [file] → #6 #7 | Privilege Escalation / T1134.001 — Access Token Manipulation: Token Impersonation/Theft | pipe name is a GUID prefix with a service-like suffix; created by a non-service process |
| 6 | CertEnrollSvc.exe calls EfsRpcEncryptFileSrv (via NdrClientCall2) over ncacn_np to `\\localhost\pipe\{efslsarpc / efsrpc / …}` with a path containing the attacker pipe name, coercing LSASS to connect | RPC call / pipe connection [network] → #7 | Privilege Escalation / T1134.001 — Access Token Manipulation: Token Impersonation/Theft | MS-EFSR RPC call originating from a non-LSASS / non-CertSvc context; crafted path encodes attacker pipe |
| 7a | CertEnrollSvc.exe impersonates the connecting LSASS client via FSCTL_PIPE_IMPERSONATE (NtFsControlFile indirect syscall), captures the SYSTEM impersonation token (NtOpenThreadToken), and duplicates it to a primary token (NtDuplicateToken) — all via trampoline page, bypassing advapi32 hook entry points | SYSTEM primary token [identity] → #8 | Privilege Escalation / T1134.001 — Access Token Manipulation: Token Impersonation/Theft | token impersonation path uses direct syscall trampolines; ImpersonateNamedPipeClient / advapi32 entry never called on x64 |
| 7b | CertEnrollSvc.exe impersonates the connecting LSASS client via FSCTL_PIPE_IMPERSONATE (NtFsControlFile indirect syscall), captures the SYSTEM impersonation token (NtOpenThreadToken), and duplicates it to a primary token (NtDuplicateToken) — all via trampoline page, bypassing advapi32 hook entry points | SYSTEM primary token [identity] → #8 | Defense Evasion / T1134.001 — Access Token Manipulation: Token Impersonation/Theft | token impersonation path uses direct syscall trampolines; ImpersonateNamedPipeClient / advapi32 entry never called on x64 |
| 8a | CertEnrollSvc.exe spawns `<target_cmd>` as NT AUTHORITY\SYSTEM via CreateProcessWithTokenW with CREATE_NO_WINDOW | SYSTEM child process [process] | Privilege Escalation / T1134.002 — Access Token Manipulation: Create Process with Token | service-account or IIS worker ancestry spawning an arbitrary child as SYSTEM |
| 8b | CertEnrollSvc.exe spawns `<target_cmd>` as NT AUTHORITY\SYSTEM via CreateProcessWithTokenW with CREATE_NO_WINDOW | SYSTEM child process [process] | Defense Evasion / T1134.002 — Access Token Manipulation: Create Process with Token | service-account or IIS worker ancestry spawning an arbitrary child as SYSTEM |
| 9 | CertEnrollSvc.exe deletes %TEMP%\*.tmp after child process exits (stdin mode only) | — [file] | Defense Evasion / T1070.004 — Indicator Removal: File Deletion | temp PE removed post-execution; eliminates disk forensic copy |
